<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="binar.abika.binarcodechallange">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".feature.RegisterActivity">
        </activity>
        <activity android:name=".feature.LoginActivity">
        </activity>
        <activity android:name=".feature.MainActivity">
        </activity>
        <activity android:name=".feature.DashActivity">
        </activity>
        <activity
                android:name=".feature.SplashScreenActivity"
                android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>